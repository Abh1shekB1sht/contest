/**
  *  AUTHOR:    unix
  *  CREATED:   27.07.2025 14:02:00
**/
#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int n, k, x;
	cin >> n >> k >> x;
	vector<string> s(n);
	for (int i = 0; i < n; i++) {
		cin >> s[i];
	}
	
	return 0;
}